# steam-market-assistan-sniper
SMA Sniper instantly navigates to a specific Steam Market listing and opens the buy dialog automatically. Just open a link with ?page=X&amp;listingid=Y&amp;count=Z parameters — the extension does the rest. If the listing isn't found on the target page, it automatically checks adjacent pages (page-1, page+1).
